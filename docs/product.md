# Product boundary

An analyst can ask one of three exact, versioned payments questions over a synthetic fixture and inspect a transparent trust verdict. The prototype refuses every other natural-language request rather than guessing.
